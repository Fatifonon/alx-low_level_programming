#include <stdio.h>

/**
 * main - print the alphabet in lowercase,
 * followed by a new line
 * Return: always 0 (success)
*/
int main(void)
{

	char i;
i = 'a';
while (i <= 'z')
putchar(i++)
putchar('\n');
return (0);
}
