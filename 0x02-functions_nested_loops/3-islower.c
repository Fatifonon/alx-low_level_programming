#include "main.h"

/**
 * int _islower(int c) - print 1 if c is in lowercase otherwise 
 * Return: 0
*/
int _islower(int c)
{
	char letter = (1);
	for (letter = 'a'; letter <= 'z'; letter++)
		_putcher(letter'\n');
return:0
}
	
